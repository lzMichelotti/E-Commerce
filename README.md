# Projeto de E-commerce para Estudo de React e JavaScript

Este é um projeto de uma loja de roupas virtual, desenvolvido com foco no aprendizado e aprofundamento em React.js e JavaScript.

## 🚀 Tecnologias Utilizadas

* **React.js:** Biblioteca principal para a construção da interface de usuário.
* **Vite:** Ferramenta de build para um desenvolvimento rápido e otimizado.
* **React Router:** Para criar uma navegação fluida no estilo Single Page Application (SPA).
* **Context API:** Utilizada para o gerenciamento de estados globais da aplicação, como o carrinho de compras.
* **Tailwind CSS:** Para uma estilização moderna e responsiva.

## ✨ Funcionalidades

*   **Catálogo de Produtos:** Navegue por uma lista de produtos carregados dinamicamente.
*   **Página de Detalhes do Produto:** Veja informações detalhadas de cada item.
*   **Carrinho de Compras:** Adicione e remova produtos do seu carrinho.
*   **Layout Responsivo:** A aplicação se adapta a diferentes tamanhos de tela, de desktops a celulares.

## 📦 Como Executar

Para executar o projeto em sua máquina local, siga estes passos:

1.  **Clone o repositório:**
    ```bash
    git clone <URL_DO_REPOSITORIO>
    ```

2.  **Acesse o diretório do projeto:**
    ```bash
    cd <NOME_DO_DIRETORIO>
    ```

3.  **Instale as dependências:**
    ```bash
    npm install
    ```

4.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```

Após esses passos, a aplicação estará disponível em `http://localhost:5173` (ou em outra porta indicada no terminal).
